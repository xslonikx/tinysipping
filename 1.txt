        self.dst_host = "",  # value is to be filled below
        self.dst_port = DFL_SIP_PORT,  # value may be redefined below
        self.bind_addr = "",
        self.bind_port = 0,
        self.count = cliargs.count,
        self.timeout = cliargs.sock_timeout,
        self.proto = cliargs.proto,  # let user
        self.verbose_mode = cliargs.verbose_mode,
        self.bad_resp_is_fail = cliargs.bad_resp_is_fail,
        self.pause_between_transmits = cliargs.pause_between_transmits,
        self.payload_size = cliargs.payload_size,
        self.dont_set_df_bit = cliargs.dont_set_df_bit,
        self.from_user = DFL_FROM_USER,
        self.to_user = DFL_TO_USER,
        self.tls_sec_level = cliargs.tls_sec_level,
        self.from_domain = None,
        self.to_domain = None,
        self.ca_certs_path = 
